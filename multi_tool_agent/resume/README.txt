Drop your resume here as `resume.pdf` (or set an absolute path in
profile/candidate.json -> "resume_path").

If a file named resume.pdf is not found, the mailer falls back to the first
*.pdf in this folder.
